// ToDo: start with manually mirrored file structure of api-dump (with manual examples in each), then insert related snippet when formatting each json file to md
// Figure out: how to pre-populate intelligently, and how to notify when examples missing/changed/etc?
    // Look at how core does it?